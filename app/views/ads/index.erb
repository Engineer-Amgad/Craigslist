<h1>Ads</h1>
<% @ads.each do |ad| %>
    <h2><a href="/ads/<%= ad.id %>"</a> <%= ad.title %> </h2>
    <p> <%= ad.price %> </p>
    <img src="<%= ad.image %>" alt="Image" height="200" width="400">
<% end %>
