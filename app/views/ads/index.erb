<h1>Ads</h1>
<a href="/ads/new"> Create a new ad </a>
<% @ads.each do |ad| %>
    <h2><a href="/ads/<%= ad.id %>"</a> <%= ad.title %> </h2>
    <p> <%= ad.description %> </p>
    <img src="<%= ad.image %>" alt="ad_image">
<% end %>
